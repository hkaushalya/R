\documentclass{article}

\usepackage{Sweave}
\begin{document}
\input{sweave_test-concordance}
library(lattice)
library(help=lattice)
package ? lattice
example(lattice)

data(environmental)
summary(environmental)
boxplot(environmental) 



\end{document}
